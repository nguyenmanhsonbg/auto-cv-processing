# Work log — 20/07/2026

**Người thực hiện:** TinBip28 (`luntanson@gmail.com`)

## Tóm tắt

- Nâng cấp cơ chế phát hiện CV trùng/lặp bằng exact file hash kết hợp với so sánh nội dung hybrid theo word n-gram, character n-gram và từng section của CV.
- Bổ sung pipeline sanitization, cleanup và rollback CV để public apply an toàn hơn.
- Cải thiện xử lý duplicate candidate, idempotency replay, rate limit và thông báo kết quả trên giao diện ứng tuyển công khai.

## Chi tiết theo commit

### 09:23 — `6fdc67d` — CV similarity hybrid và clean similarity gate

- Nâng cấp `CvSimilarityService` từ word TF-IDF n-gram lên phương pháp kết hợp word features, character n-grams và section similarity.
- Thêm trọng số theo section CV, exact normalized-text detection, hash và method version cho kết quả similarity.
- Bổ sung luồng đọc CV đã sanitize để tính similarity, xóa file clean tạm sau khi parse và quản lý file ở quarantine/safe storage.
- Thêm khả năng xóa các version CV cũ, rollback public upload và tìm CV theo original file hash.
- Cải thiện nhận diện candidate hiện có bằng tổ hợp name + email + phone; chuẩn hóa tên Unicode/whitespace/case.
- Điều chỉnh public apply rate limit về cửa sổ 15 giây.
- Cập nhật public apply controller/UI và API error handling để phản ánh kết quả kiểm tra CV.
- Dọn các file CV quarantine cũ không còn cần thiết.
- Bổ sung/cập nhật test và tài liệu về hybrid similarity, result display, clean similarity gate và hash-first similarity.
- Thay đổi chính: 24 file, `+1837 / -91` dòng.

### 16:23 — `25fbef1` — Tinh chỉnh similarity V3 và idempotency replay

- Nâng method version similarity lên `TFIDF_WORD_CHAR_SECTION_V3`.
- Chuẩn hóa thứ tự và label section trước khi so sánh; nhận diện thêm section `personal projects`.
- Bổ sung tra cứu CV theo idempotency key để phân biệt replay hoàn tất với public reapply cần rollback.
- Cập nhật public apply controller để chỉ rollback khi phù hợp và giữ nguyên CV hiện tại trong trường hợp file đã tồn tại.
- Cập nhật giao diện public apply: hiển thị rõ “trùng file” thay vì “similarity”, không reset form khi CV bị từ chối do đã tồn tại.
- Bổ sung/cập nhật test liên quan đến CV documents, similarity và public job postings.
- Thay đổi chính: 7 file, `+154 / -25` dòng.

## Tổng kết

- **Số commit:** 2
- **Khoảng thời gian:** 09:23–16:23 (GMT+7)
- **Tổng thay đổi theo commit:** 31 file, `+1991 / -116` dòng
- **Kết quả:** Hoàn thiện lớp kiểm tra CV trùng/lặp đáng tin cậy hơn, bảo vệ luồng public apply và hiển thị đúng trạng thái cho người dùng.

## Ghi chú truy vết

- Commit message gốc của cả hai commit là `commit`; nội dung work log được tổng hợp từ file thay đổi và diff.
- Work log này được tạo từ lịch sử Git của repository `auto-cv-processing`.
