services:
  postgres:
    image: postgres:14-alpine
    environment:
      POSTGRES_DB: interview_assistant
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5434:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: Dockerfile
      target: backend
    ports:
      - '3000:3000'
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/interview_assistant
      JWT_SECRET: change-me-in-production
      NODE_ENV: development
      PORT: 3000
      CV_PDF_SANITIZER_MODE: GHOSTSCRIPT_DOCKER
      CV_GHOSTSCRIPT_DOCKER_IMAGE: auto-cv-processing-ghostscript:latest
      CV_GHOSTSCRIPT_TIMEOUT_MS: 60000
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - uploads:/app/uploads

  cv-sanitizer:
    image: auto-cv-processing-ghostscript:latest
    build:
      context: .
      dockerfile: Dockerfile
      target: ghostscript-sanitizer
    profiles:
      - sanitizer
    command: ['gs', '--version']

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: frontend
      args:
        VITE_API_URL: /api
    ports:
      - '3001:80'
    depends_on:
      - backend

volumes:
  pgdata:
  uploads:
